#!/usr/bin/env ruby
require 'rubygems'
require 'sidekiq'
require_relative '../lib/migration/worker'

RUNNER_PATH = File.join(ROOT_PATH, 'lib', 'migration', 'runner.rb')

def queue
  Migration::Worker.config.queue
end

system "sidekiq #{queue} -c 1 -r #{RUNNER_PATH}"
